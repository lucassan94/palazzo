# === Banco de Dados ===
DB_HOST=86.48.18.22
DB_PORT=5432
DB_NAME=delivery
DB_USER=default
DB_PASS=default

# === JWT ===
JWT_SECRET=coloque-uma-chave-segura-aqui-com-pelo-menos-32-caracteres
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# === RESTAURANTE (Multi-tenant) ===
# Altere este ID para cada instância do restaurante
RESTAURANT_ID=1

# === SERVER ===
PORT=3001
NODE_ENV=production
CORS_ORIGIN=http://localhost:5173,http://localhost:5174,http://localhost:5175

# === UPLOAD ===
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=5242880
